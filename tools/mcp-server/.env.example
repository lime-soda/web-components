# MCP Server Configuration

# Glob pattern for custom element manifest files
# Default: packages/*/custom-elements.json
CUSTOM_ELEMENTS_MANIFEST_GLOB=packages/*/custom-elements.json

# Base path for resolving relative paths
# Default: ../../ (relative to the mcp-server directory)
WORKSPACE_ROOT=../../

# Optional: Tokens directory path
# Default: support/tokens
TOKENS_PATH=support/tokens