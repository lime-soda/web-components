/* * The semantic token tier, which an application is expected to load. Without it * every
`--theme-*` reference is undefined and the component renders unstyled. */ import
'@lime-soda/tokens/variables.css';