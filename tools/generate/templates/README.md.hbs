# {{titleCase name}}

## Install

```shell
npm install --save @lime-soda/{{name}}
```

## Usage

Use {{lowerCase name}} with your favourite bundler or directly from a CDN.

### Bundler

Import the `{{pascalCase name}}` component in the root of your application.

```js
import '@lime-soda/{{name}}'
```

### CDN

Import the `{{pascalCase name}}` component from a CDN.

```html
<script type="module" src="https://esm.sh/@lime-soda/{{name}}"></script>
```
