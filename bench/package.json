{
  "name": "@tradeflow/bench",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "bench": "vitest run",
    "bench:node": "vitest run --project node",
    "bench:browser": "vitest run --project browser",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tradeflow/core": "workspace:*"
  },
  "devDependencies": {
    "@vitest/browser": "^3.2.4",
    "playwright": "^1.56.1",
    "typescript": "~5.9.3",
    "vitest": "^3.2.4"
  }
}
